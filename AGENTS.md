We follow @docs/implementation_principles.md whenever working on this library.

This repo uses `jj` as VCS. Please `jj describe` and `jj new` between each significant change.

We use `backlog` command for managing backlog tickets. See docs/BACKLOG.md for more information.

